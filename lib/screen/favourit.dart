import 'package:flutter/material.dart';

class faviourit extends StatelessWidget {
  const faviourit({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          children: const [Text('hey im faviourit ')],
        ),
      ),
    );
  }
}
